# AGENTS.md

## Cursor Cloud specific instructions

This repository (`clearpath-eval`) is currently an empty/newly initialized project with no source code, dependencies, or services. The repo contains only a `README.md`.

- **No build/lint/test commands** are available yet. Once code is added, update this section with the relevant commands.
- **No external services** (databases, caches, queues) are required.
- **No package manager** lockfile or manifest exists. When one is added, the VM update script should be updated accordingly.
